<h3>You are logged in as <%= current_user.username %></h3>
<p>
    <strong><%= @category.nil? ? "All " : @category.name %>
    available services:</strong>
    <% @services.each do |service| %>
        <% @offer_user = service.offered_user %>
        <li><%= link_to service.title, @category.nil? ? service_path(service) : category_service_path(@category, service) %> by <%= service.user.nil? ? "" : service.user.username %>, assigned to <%= @offer_user.nil? ? "" : @offer_user.username %></li>
    <% end %>
</p>